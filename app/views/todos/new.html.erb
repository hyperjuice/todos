<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>new</title>
</head>
<body>
	<%= form_for :todo, url: '/todos', method: 'post' do |f| %>
		<%= f.text_area :description %>
		<%= f.submit 'save To-Do' %>
	<% end %>	
</body>
</html>