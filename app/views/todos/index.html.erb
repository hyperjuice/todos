<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>index</title>
</head>
<body>
	<% @todos.each do |todo| %>
		<div>
			To-Do Description: <%= todo.description %>	
		</div>
	<% end %>	

	<br>

	<%= form_for :todo, url: '/todos', method: 'post' do |f| %>
		<%= f.text_area :description %>
		<%= f.submit 'save To-Do' %>
	<% end %>	
</body>
</html>